#
# widget layout configuration, avalaible widgets: 
#  > sysinfo, services, lxc, pkgs, net, certs, disk, last, backup
# for widget configuration, edit widget file
#

# col=2
sysinfo   net
services  certs
lxc       disks
pkgs      last

