#
# widget layout configuration, avalaible widgets: 
#   > apt, backup, certs, disks, docker, lastexec, lxc, network,
#     pacman, partitions, services, sysinfo
# make sure you don't delete the trailing quotation mark.
#
layout="
sysinfo   network
services  partitions
lxc       apt
-         certs
-         lastexec
"

col=2 # motd columns

docker_columns=2
docker_filter='nothing'  # excluded containers separated by |

# lxc widget
lxc_columns=2
lxc_filter='nothing' # excluded containers separated by |

# partitions widget
disk_warn=80 # %
partitions_filter='nothing' # excluded target separated by |

# services widget
services_columns=2
services=( 'rsyslog' 'cron' 'logrotate.timer' 'sshd' 'networking')
svc_names=('syslog'  'cron' 'logrotate'       'sshd' 'network'  )

# sysinfo widget
mem_warn=50 # %
cpu_warn=20 # %
temp_warn=60 # °c